**User ager render krito UI er event handler e click kore, click er sathe sathe inplaced state updaye hoye zayna. User er clicker jonno ze state update hoy ta porer render e pawya zay.**

**Render means return of new UI according to the new passes param/arguament to the component function**

How JSX code executes and behave by state changes?
Before a componnent displayed on screen they must be rendered by React.

Ekta component and er attributes JSX code display hote 3 ti step follow kore.
State change hole ekta re-render er request hoy.....
1. Trigger: web page dekhanu ba serve korar request ( user interactivity te state change er porer obosta ) App protombar start korar jonno ekta initial render trigger hoy, tarpor zokhon component er state update hoy tokhon automatic render trigger hoy. Satte update er render gulu queue te joma thake.

2. Rendering: Recursive process. React render the component ( find out the difference between two state changes and React call a component to figuare out what to display on screen )
Initial render a React rootComponent ke call kore. Initial rendeing DOM nodes create hoy.
Subsequent render e React component er ze state function er jonno render triggered hoiche sei state function ke call kore and ager state er sathe difference khuje ber kore. Subsequent rendering e DOM nodes create hoy na shudhumatro proujonio nodes gulu surgically push hoy

3. Commit: React final kore ze DOM e ki ki poriborton asbe (  Rendering e JSX modify hoy and modified JSX(updated component) actual DOM e display hoy )
React changes the DOM nodes if there is a difference between renders.

Epilogue (Browser rendering/painting): Zokhon rendering shesh hoy and DOM update hoy zay, updated components JSX element paint hoy ebong shesh web page e dekhte pai.