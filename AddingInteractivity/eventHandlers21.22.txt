Event handler application er nijosso ekta function zeta user interactivity te triggered hoy ebong state change korte pare.
Event handler gulu props hisabe JSX element gulute patathe hoy.
Event handler component er props er access pay and props hisabe onno ekta component e handler patanu zay.
<Button onClick={() => alert('Uploading!')}>
      Upload Image
</Button>, ekhane onClick ekta event handler tobe Button component e props hisabe pathanu hoyeche.
function Button({props,children}) {
   return <button onClick={props.onClick}>{children}</button>
   button JSX element e "Upload Image" text children hisabe show hobe.
}
Pass Event Handler as a Props Module-2.1(10-22)minitue

Event Propagation 2.2: Bivinno direction e event colacol kora.
Event Bubble, Deligation and Capturing mile event propagation create kore.
Shudhumatro onScroll event propagate hoyna. onno sob event propagate hoy.

function Button({onSmash,children}){
    return <button 
    onClick={
        (e)=>{ e.stopPropagation(),
               onSmash
        //ekhane button e event handler onClick e onSmash er reference deya zeto. Kintu er sathe event e er propagation stop korte hobe, tai ekti anonymous function likhe ekadik kaj/function ke wrap kore deya zay.
              
        }
    }
        > {children} </button>
}
Event handlers don't need to be pure, we should mange application/component side effects in event handler's function.

